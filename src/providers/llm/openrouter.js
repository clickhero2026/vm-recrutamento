'use strict';

// Adaptador LLM: OpenRouter (gateway padrao - uma chave, varios modelos).
// STUB da Fase 0: assinatura final, sem chamada real. Implementacao na Fase 3.

async function completar(_mensagens, _opcoes = {}) {
  throw new Error('Provedor LLM "openrouter" ainda nao implementado - Fase 3.');
}

module.exports = { completar };
